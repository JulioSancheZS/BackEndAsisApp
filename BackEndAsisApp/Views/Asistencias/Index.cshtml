@model IEnumerable<BackEndAsisApp.Models.Asistencia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Alumno.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Docente.Apellido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmunAsi.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Alumno.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Docente.Apellido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmunAsi.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAsistencia }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAsistencia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAsistencia })
        </td>
    </tr>
}

</table>
